version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: stock_exchange_db
    environment:
      POSTGRES_USER: stock_user
      POSTGRES_PASSWORD: secure_password_123
      POSTGRES_DB: stock_exchange
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./initdb:/docker-entrypoint-initdb.d  # For initial schema setup
    ports:
      - "5432:5432"
    networks:
      - stock_net
    restart: unless-stopped

  redis:
    image: redis:7-alpine
    container_name: stock_exchange_redis
    command: redis-server --appendonly yes
    volumes:
      - redisdata:/data
    networks:
      - stock_net
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: stock_exchange_backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgres://stock_user:secure_password_123@db:5432/stock_exchange
      REDIS_URL: redis://redis:6379
      YAHOO_FINANCE_API: https://query1.finance.yahoo.com/v8/finance/chart
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - stock_net
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 5

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: stock_exchange_frontend
    ports:
      - "8080:80"
    environment:
      VUE_APP_API_BASE_URL: http://localhost:8000
    networks:
      - stock_net
    restart: unless-stopped

networks:
  stock_net:
    driver: bridge

volumes:
  pgdata:
    driver: local
  redisdata:
    driver: local